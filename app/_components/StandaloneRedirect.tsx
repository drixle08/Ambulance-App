// StandaloneRedirect is intentionally a no-op.
// The landing page (/) is the intended entry point for the PWA —
// users navigate from there using quick-action tiles or the bottom nav.
export function StandaloneRedirect() {
  return null;
}
